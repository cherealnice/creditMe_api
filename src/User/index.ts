import { default as typeDefs } from './typeDefs';
import { default as resolvers } from './resolvers';
import { default as permissions } from './permissions';

export default { typeDefs, resolvers, permissions };
